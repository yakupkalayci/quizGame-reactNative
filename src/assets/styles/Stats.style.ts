import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        borderTopColor: '#9a2af0',
        borderTopWidth: 2,
        margin:10,
        padding: 10,
        justifyContent: 'space-between'
    },
    textWeight: {
        fontWeight: 'bold',
        color: '#000'
    },
    textSize: {
        fontSize: 15,
    },
    textContainer: {
        flexDirection: 'row'
    },
    button: {
        width: '100%',
    }
});
